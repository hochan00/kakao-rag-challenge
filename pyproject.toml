[project]
name = "kakao-rag-challenge"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.13"
dependencies = [
    "numpy>=2.5.2",
    "pytest>=9.1.1",
    "scikit-learn>=1.9.0",
]

[tool.pytest.ini_options]
pythonpath = ["src"]
