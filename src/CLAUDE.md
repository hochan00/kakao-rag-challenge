🔗 결과기 규칙

반드시 지킬 것
answer_question(question: str) 함수명과 입력 형식 유지
반환값에 answer와 retrieved 포함, retrieved는 관련도 순 1개에서 4개
전역 FastAPI app, GET /health, POST /answer 제공
생성형 LLM은 Qwen2.5-Instruct 계열
새 Colab T4 런타임에서 위에서 아래로 한 번 실행되면 끝나는 자기완결형 파일
1번 셀 팀 구현 + 2번 셀 공통 러너의 두 셀 구성
문항 수를 코드에 고정하지 않기. 같은 제출본으로 공개 10문항과 비공개 30문항을 모두 처리해야 합니다
답변 JSON은 공통 러너가 만듭니다. 직접 조립하거나 수정하지 않습니다

자유롭게 정할 것
1번 셀 안의 구현 방법과 코드 구성
필요한 패키지 설치
Qwen2.5-Instruct 계열 안에서 모델 크기, 미세조정 방식, 양자화, 생성·추론 설정
프롬프트, 검색·리랭킹 등 RAG 구성과 FastAPI 서빙 구현 방식
약관 원문을 확보하고 가공하는 방법

쓸 수 없는 것
Google Drive 마운트, 미리 업로드해 둔 파일, 개인 PC 경로에 의존하는 코드
외부 생성형 LLM API, 원격 임베딩·리랭커, 원격 관리형 검색 서비스
실행 중 사람 입력을 기다리는 코드(input() 등)
torch 재설치, torch.compile
질문과 상관없이 약관 전문을 매 질문 프롬프트에 통째로 넣는 방식
공통 러너의 문항·측정·저장 로직 수정

앞의 두 가지가 특히 중요합니다.
운영진은 팀 파일이 하나도 없는 새 세션에서 여러분의 코랩을 실행합니다.
드라이브에 올려 둔 파일에 의존하고 있으면 그 세션에서 전부 실패합니다.
